# Dependencies
node_modules/
.aws-sam/

# Env / secrets (never commit)
# Note: .env files with VITE_* vars are safe to commit (they're public in the JS bundle anyway)
# Only .env.local is gitignored (for machine-specific overrides)
.env.local
.env.*.local
*.pem
frontend/property-site/js/maps-config.js

# Build artifacts
dist/
build/
.build/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
.claude/settings.local.json

# Local recovery and test artifacts
.recovery-backend-node_modules-*/
backend/coverage/
backend/package-lock.broken-*.json

# AWS SAM build cache
.aws-sam/
samconfig.toml.bak
