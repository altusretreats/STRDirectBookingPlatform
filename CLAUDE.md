# Altus Retreats — Claude Instructions

The authoritative project instructions are in [`AGENTS.md`](AGENTS.md). Read that file in full before working in this repository and follow it for the current stack, architecture, deployment commands, property-site behavior, data boundaries, secrets conventions, resource IDs, and project status.

This file intentionally delegates to `AGENTS.md` so parallel assistant-specific instruction files cannot drift apart. Update `AGENTS.md` whenever the stack, architecture, secrets, deployment workflow, or key decisions change, then commit the documentation with the related work.

Last reviewed: 2026-08-12 after the live property-site widget and mobile booking-navigation release.
